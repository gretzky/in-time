interface Theme {
  colors: {
    primary: string;
    secondary: string;
    white: string;
    black: string;
  };
}

const theme: Theme = {
  colors: {
    primary: "#3EA6EF",
    secondary: "#FF3860",
    white: "#fff",
    black: "#000"
  }
};

export default theme;
